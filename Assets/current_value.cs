using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class current_value : MonoBehaviour
{
    /*
   // textMesh = (TextMesh) descriptionObject.transform.GetComponent(typeof(TextMesh));
    //textMesh.text = "Name : ABC";
    public cube1 source;
    int val;
    TextMesh my_text = GameObject.Find("score").GetComponent<TextMesh>();
    // Start is called before the first frame update
    void Start()
    {
        my_text.text = "fgn";
        val = source.score;
        val *= 10;
        my_text.text = val.ToString();
    }

    // Update is called once per frame
    void Update()
    {
        val = source.score;
        val *= 10;
        my_text.text = val.ToString();
    }
    


    //define a Textmesh that we want to edit 
    public TextMesh tm;
    public cube1 source;
    int val;
    // here in start method (run at instantiating of script) i find component of type
    // TextMesh ( <TextMesh> ) of object named"nameOfTheObject" and reference it
    // via tm variable;
    void Start()
    {
        tm = (TextMesh)GameObject.Find("score").GetComponent<TextMesh>();
        val=source.score;
        //string s = val.ToString();
        // here we change the value of displayed text
        tm.text = "lol";
    }
    */
}
